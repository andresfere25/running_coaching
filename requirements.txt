# Running Coaching — dependencias Python
# Instalar: pip install -r requirements.txt

# Data
pandas>=2.0.0
duckdb>=0.10.0

# Google Sheets
gspread>=6.0.0
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0

# Strava / HTTP
requests>=2.31.0

# PDF Report (legado/fallback — el objetivo futuro es una app/web)
reportlab>=4.0.0
matplotlib>=3.7.0

# Config
python-dotenv>=1.0.0

# Backend API (Fase 1)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# ─── Storage externo (Fase A/B — dual write + fuente primaria) ───────────────
supabase>=2.0.0

# ─── ML (Nivel 2/3/4 — cohorte RUNA) ─────────────────────────────────────────
# Pin exacto: los pkl de N2 (nivel2_v1.pkl) y N3 (nivel3_v1.pkl) se serializaron
# con sklearn 1.5.2. Con otra versión sklearn imprime InconsistentVersionWarning
# y puede dar predicciones sutilmente distintas. Para reproducibilidad estricta
# el contenedor Railway debe usar la misma versión que el entrenamiento.
scikit-learn==1.5.2

# ─── Dependencias futuras (Fase B+) ──────────────────────────────────────────
# anthropic>=0.40.0     # Claude API — para observaciones personalizadas
