# Running Coaching — dependencias Python
# Instalar: pip install -r requirements.txt

# Data
pandas>=2.0.0
duckdb>=0.10.0

# Google Sheets
gspread>=6.0.0
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0

# Strava / HTTP
requests>=2.31.0

# PDF Report (legado/fallback — el objetivo futuro es una app/web)
reportlab>=4.0.0
matplotlib>=3.7.0

# Config
python-dotenv>=1.0.0

# Backend API (Fase 1)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# ─── Storage externo (Fase A — dual write) ───────────────────────────────────
# Descomenta cuando tengas SUPABASE_URL y SUPABASE_SERVICE_KEY configurados
# supabase>=2.0.0       # Cliente Supabase — instalar: pip install supabase>=2.0.0

# ─── Dependencias futuras (Fase B+) ──────────────────────────────────────────
# anthropic>=0.40.0     # Claude API — para observaciones personalizadas
